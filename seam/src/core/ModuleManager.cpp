#include "core/ModuleManager.h"

namespace seam {
	void ModuleManager::add_module(std::string module_name, std::string file_path) {
		/*if (modules_.find(module_name) != modules_.cend()) {
			return; // TODO: Check whether module already exists.
		}
		
		auto module = std::make_unique<Module>(module_name, file_path);
		modules_.emplace(std::move(module_name), module);*/
	}
}